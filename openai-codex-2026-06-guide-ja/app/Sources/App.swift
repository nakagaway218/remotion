// App.swift — アプリの入口（テンプレート）
// {{APP_NAME}} は AGENTS.md STEP 3 で実際のアプリ名に置換してください（例: Mainichi）。
import SwiftUI

@main
struct {{APP_NAME}}App: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
